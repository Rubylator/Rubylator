.jumbotron style="border: solid 2px #00AAFF;"
  .page-header
    h1
      |Rubylator
      br
      small = t('.subtitle')
- unless user_signed_in?
  ul class="pager"
    li = link_to raw(icon('user', t('general.create_account'))), new_user_registration_path
    li = link_to raw(icon('sign-in', t('general.login'))), new_user_session_path
